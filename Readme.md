### API DOCUMENT
+ http://localhost:8080/swagger-ui/index.html#/


### Build and Run Project 
+ open src/main/resource/application.yaml; then change the port, schema's name, username, password for your configuration
+
+ cd clothingStore
+ ./gradlew clean build -x test
+ ./gradlew bootRun